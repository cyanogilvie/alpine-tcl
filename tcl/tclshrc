if {$tcl_interactive} {
	package require common_sighandler
	package require rl_json
	package require chantricks
	package require tclreadline
	package require aws
	package require docker

	namespace import ::rl_json::*
	namespace import ::chantricks::*

	tclreadline::readline historyexpansion no
	tclreadline::Loop
}

# vim: ft=tcl
